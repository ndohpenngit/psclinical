% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps_continuous_parallel.R
\name{ps_continuous_parallel}
\alias{ps_continuous_parallel}
\title{Continuous outcome: two-sample parallel design}
\usage{
ps_continuous_parallel(
  delta,
  sd,
  type = c("superiority", "noninferiority", "equivalence"),
  margin = NULL,
  power = NULL,
  n1 = NULL,
  n2 = NULL,
  sig.level = 0.05,
  ratio = 1,
  alternative = c("two.sided", "one.sided")
)
}
\arguments{
\item{delta}{Expected mean difference between treatment groups.}

\item{sd}{Common standard deviation.}

\item{type}{Type of test: "superiority", "noninferiority", or "equivalence".}

\item{margin}{Noninferiority or equivalence margin (required for NI/equivalence).}

\item{power}{Desired power (0-1). Provide either \code{power} or \code{n1/n2}.}

\item{n1, n2}{Sample sizes per group. Provide either \code{power} or \code{n1/n2}.}

\item{sig.level}{Type I error (default 0.05).}

\item{ratio}{Allocation ratio n1/n2 (default 1).}

\item{alternative}{"two.sided" or "one.sided" (default "two.sided").}
}
\value{
A list of class \code{ssize_result} with computed sample sizes, total, and/or achieved power.
}
\description{
Computes sample size or power for a two-sample continuous outcome trial.
}
\examples{
# Sample size determination for superiority design
ps_continuous_parallel(delta = 2, sd = 5, power = 0.8)

# Power estimation for given sample sizes
ps_continuous_parallel(delta = 2, sd = 5, n1 = 50, n2 = 50)

# Noninferiority design: sample size determination
ps_continuous_parallel(delta = 1, sd = 2, type = "noninferiority",
                       margin = 0.5, power = 0.9)

# Equivalence design: power estimation
ps_continuous_parallel(delta = 1, sd = 2, type = "equivalence",
                       margin = 0.5, n1 = 60, n2 = 60)
}
