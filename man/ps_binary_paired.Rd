% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps_binary_paired.R
\name{ps_binary_paired}
\alias{ps_binary_paired}
\title{Sample size or power for paired binary outcome (McNemar test approximation)}
\usage{
ps_binary_paired(
  p1,
  p2,
  rho = 0.5,
  n = NULL,
  power = NULL,
  sig.level = 0.05,
  alternative = c("two.sided", "one.sided")
)
}
\arguments{
\item{p1}{Numeric. Event probability at baseline.}

\item{p2}{Numeric. Event probability at follow-up.}

\item{rho}{Numeric. Correlation between paired responses (default 0.5).}

\item{n}{Numeric. Sample size if computing achieved power.}

\item{power}{Numeric. Desired power if computing required sample size.}

\item{sig.level}{Numeric. Type I error (default 0.05).}

\item{alternative}{Character. "two.sided" or "one.sided".}
}
\value{
List of class \code{ssize_result} with either:
\itemize{
\item \code{n} - required sample size (if \code{power} was provided)
\item \code{achieved_power} - achieved power (if \code{n} was provided)
}
}
\description{
Computes either the required sample size or the achieved power
for a paired binary design using McNemar's test or correlated proportions.
}
\examples{
# Required sample size for McNemar test
ps_binary_paired(p1 = 0.3, p2 = 0.5, power = 0.8)

# Achieved power for given sample size
ps_binary_paired(p1 = 0.3, p2 = 0.5, n = 60)
}
