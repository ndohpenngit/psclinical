% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps_continuous_paired.R
\name{ps_continuous_paired}
\alias{ps_continuous_paired}
\title{Sample size or power for paired continuous outcome}
\usage{
ps_continuous_paired(
  delta,
  sd_diff,
  n = NULL,
  power = NULL,
  sig.level = 0.05,
  alternative = c("two.sided", "one.sided")
)
}
\arguments{
\item{delta}{Numeric. Expected mean difference between paired measurements.}

\item{sd_diff}{Numeric. Standard deviation of paired differences.}

\item{n}{Numeric. Sample size if computing achieved power.}

\item{power}{Numeric. Desired power if computing required sample size.}

\item{sig.level}{Numeric. Type I error (default 0.05).}

\item{alternative}{Character. "two.sided" or "one.sided".}
}
\value{
List of class \code{ssize_result} with either:
\itemize{
\item \code{n} - required sample size (if \code{power} was provided)
\item \code{achieved_power} - achieved power (if \code{n} was provided)
}
}
\description{
Computes either the required sample size or the achieved power
for a paired continuous outcome (e.g., pre-post measurements).
}
\examples{
# Required sample size for paired continuous design
ps_continuous_paired(delta = 2, sd_diff = 3, power = 0.8)

# Achieved power for given sample size
ps_continuous_paired(delta = 2, sd_diff = 3, n = 50)
}
